'use client'

export interface PhotoData{
   key:any,
    image: string,
    thumbnail: string,
    name: string,
    label: string,
    coordinates: any,
    classId: any,
    className: string,
    polygCount: string,
}